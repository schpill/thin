<?php if ($paginator->last && $paginator->perPage <= $paginator->total): ?>
    <div class="alert alert-success">Enregistrements <?php echo $paginator->firstItemNumber; ?> à <?php echo $paginator->lastItemNumber; ?> sur <?php echo $paginator->total; ?></div>

    <?php echo $paginator->links(); ?>
<?php endif; ?>

